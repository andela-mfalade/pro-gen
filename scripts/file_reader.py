"""Read text files and extract contents.

Read a given text file, parse and format its contents, then return the result
"""



def extract_file_contents(file_path):
    with open(file_path, 'r') as f:
        import ipdb; ipdb.set_trace()
